import { Component } from '@angular/core';

/** Empty state for the root URL, before any airport has been chosen. */
@Component({
  selector: 'app-landing',
  standalone: false,
  templateUrl: './landing.html',
})
export class Landing {}
